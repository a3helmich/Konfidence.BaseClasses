# Konfidence.UtilHelper

Some obsolete classes. Technical debt :(

Kept because older software of mine still uses them. Not recommended for new code — the framework has better answers for all of it now.

Part of the [Konfidence.BaseClasses](https://github.com/a3helmich/Konfidence.BaseClasses) collection of libraries.

## What is in it

- **`ApplicationSettings` / `IApplicationSettings` / `ApplicationSettingsFactory`** — legacy `.settings` XML file get/set-string-value store
- **`BaseApplicationConfiguration`** — legacy XML app.config-style read/write of string/bool/byte-array node values
- **`BaseXmlDocument`** — thin XML document base helper

Targets **net9.0** and **net10.0**.

## Full documentation

The other libraries in the collection, and build/test instructions, are in the
[README on github.com](https://github.com/a3helmich/Konfidence.BaseClasses#readme).
